//-----------------------------------------------------------------------------
// String Extensions
declare interface String
{
	contains(string:string):boolean;
	format():string;
	padZero(length:number):string;
}